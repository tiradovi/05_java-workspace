package edu.oop.constructor;

public class MemberRun {
    public static void main(String[] args) {
        // html에서 사용자에게 전달할 데이터나 사용자가 작성한 데이터를 getter, setter를 활용하여 전달 기능
        Member m = new Member();

        // html에서 기입한 정보 가져오기
        // 가입할 때, 이미 존재하는 email이 존재하는지 확인하고 저장
/*
        if ("데이터베이스에서 email 컬럼에 사용자가 작성한 이메일이 존재한다면" == "") {
            return "이미 존재하는 이메일 입니다";
        }
        // 존재하지 않는 다면
        m.setEmail(); //그 후저장
        if ("데이터베이스에서 nickname 컬럼에 사용자가 원하는 닉네임을 누군가 이미 쓰고 있다면" == "") {
            return "이미 사용중인 닉네임입니다.";
        }
        // 존재하지 않는다면
        m.setNickname();// 그후 저장

 */

    }
}
